const localAssets = require('./local-assets');
const packageURL = require('./package-url');
const getDefaults = require('./get-defaults');
const files = require('./files');

module.exports = { localAssets, packageURL, getDefaults, files };
